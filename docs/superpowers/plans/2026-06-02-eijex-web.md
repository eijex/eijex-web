# eijex-web Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** www.eijex.com 단일 랜딩 페이지 구축 및 Vercel 배포

**Architecture:** Next.js 15 App Router 기반 정적 단일 페이지. 섹션별 컴포넌트로 분리하여 향후 유지보수 용이. Tailwind CSS로 반응형 처리.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS, Vercel

**Spec:** `docs/superpowers/specs/2026-06-02-eijex-web-design.md`

---

### Task 1: Next.js 프로젝트 스캐폴드

**Files:**
- Create: `package.json`, `tsconfig.json`, `tailwind.config.ts`, `next.config.ts`, `postcss.config.mjs`
- Create: `src/app/layout.tsx`
- Create: `src/app/globals.css`
- Create: `.gitignore`

- [ ] **Step 1: Next.js 프로젝트 생성**

`c:\Work\eijex\eijex-web` 폴더 안에서 실행:

```bash
cd c:/Work/eijex/eijex-web
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack
```

프롬프트가 나오면 모두 기본값(Enter).  
이미 `docs/` 폴더가 있어도 충돌하지 않음.

- [ ] **Step 2: 불필요한 보일러플레이트 제거**

```bash
rm src/app/page.tsx   # 뒤에서 새로 작성
```

`src/app/globals.css` 내용을 아래로 교체:

```css
@import "tailwindcss";
```

- [ ] **Step 3: 폰트 설정 (layout.tsx)**

`src/app/layout.tsx` 전체 교체:

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "eijex — Open tools for plant synthetic biology",
  description:
    "eijex builds open-source computational tools for plant synthetic biology — peer-reviewed, reproducible, and freely available to the research community.",
  openGraph: {
    title: "eijex",
    description: "Open tools for plant synthetic biology.",
    url: "https://www.eijex.com",
    siteName: "eijex",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
```

- [ ] **Step 4: dev 서버 실행 확인**

```bash
npm run dev
```

브라우저에서 `http://localhost:3000` 접속 — Next.js 기본 페이지 또는 404가 뜨면 정상.

- [ ] **Step 5: 커밋**

```bash
git init
git add .
git commit -m "chore: scaffold Next.js 15 project for eijex-web"
```

---

### Task 2: Nav 컴포넌트

**Files:**
- Create: `src/app/components/Nav.tsx`

- [ ] **Step 1: Nav 컴포넌트 작성**

```tsx
// src/app/components/Nav.tsx
export default function Nav() {
  return (
    <nav className="bg-white border-b border-slate-200 sticky top-0 z-50">
      <div className="max-w-5xl mx-auto px-6 py-4 flex justify-between items-center">
        <span className="font-extrabold text-lg bg-gradient-to-r from-teal-400 to-green-900 bg-clip-text text-transparent">
          eijex
        </span>
        <div className="flex items-center gap-6 text-sm text-slate-500">
          <a href="#products" className="hover:text-slate-900 transition-colors hidden sm:block">
            Products
          </a>
          <a href="#about" className="hover:text-slate-900 transition-colors hidden sm:block">
            About
          </a>
          <a
            href="https://github.com/eijex"
            target="_blank"
            rel="noopener noreferrer"
            className="hover:text-slate-900 transition-colors hidden sm:block"
          >
            GitHub
          </a>
          <a
            href="https://factorforge.eijex.com"
            target="_blank"
            rel="noopener noreferrer"
            className="bg-slate-900 text-white px-4 py-2 rounded-full text-xs font-semibold hover:bg-slate-700 transition-colors"
          >
            Get started →
          </a>
        </div>
      </div>
    </nav>
  );
}
```

- [ ] **Step 2: page.tsx에서 임시 확인**

`src/app/page.tsx` 생성:

```tsx
import Nav from "@/app/components/Nav";

export default function Home() {
  return (
    <main>
      <Nav />
    </main>
  );
}
```

`http://localhost:3000` 에서 Nav 렌더링 확인.

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/Nav.tsx src/app/page.tsx
git commit -m "feat: add Nav component"
```

---

### Task 3: AboutStrip 컴포넌트

**Files:**
- Create: `src/app/components/AboutStrip.tsx`

- [ ] **Step 1: AboutStrip 작성**

```tsx
// src/app/components/AboutStrip.tsx
export default function AboutStrip() {
  return (
    <div id="about" className="bg-teal-50 border-b border-teal-100 py-4 px-6 text-center">
      <p className="text-sm text-teal-700 leading-relaxed max-w-2xl mx-auto">
        eijex builds open-source computational tools for plant synthetic biology —
        peer-reviewed, reproducible, and freely available to the research community.
      </p>
    </div>
  );
}
```

- [ ] **Step 2: page.tsx에 추가**

```tsx
import Nav from "@/app/components/Nav";
import AboutStrip from "@/app/components/AboutStrip";

export default function Home() {
  return (
    <main>
      <Nav />
      <AboutStrip />
    </main>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/AboutStrip.tsx src/app/page.tsx
git commit -m "feat: add AboutStrip component"
```

---

### Task 4: Hero 컴포넌트

**Files:**
- Create: `src/app/components/Hero.tsx`

- [ ] **Step 1: Hero 작성**

```tsx
// src/app/components/Hero.tsx
export default function Hero() {
  return (
    <section
      className="py-20 px-6 text-center"
      style={{
        background: "linear-gradient(160deg, #f0fdfa 0%, #f8fafc 55%, #f0fdf4 100%)",
      }}
    >
      <div className="max-w-2xl mx-auto">
        <span className="inline-block bg-green-100 text-green-800 text-xs font-bold px-3 py-1 rounded-full tracking-widest mb-6">
          OPEN SOURCE · PLANT SYNTHETIC BIOLOGY
        </span>
        <h1 className="text-4xl sm:text-5xl font-extrabold text-slate-900 leading-tight tracking-tight mb-4">
          Open tools for<br />plant synthetic biology.
        </h1>
        <p className="text-base text-slate-500 mb-10 leading-relaxed">
          From sequence to expression — reproducibly.<br />
          Open-source, citable, and benchmarked.
        </p>
        <div className="flex flex-col sm:flex-row justify-center gap-3">
          <a
            href="https://factorforge.eijex.com"
            target="_blank"
            rel="noopener noreferrer"
            className="bg-teal-600 text-white px-6 py-3 rounded-full text-sm font-bold hover:bg-teal-700 transition-colors"
          >
            Explore FactorForge →
          </a>
          <a
            href="https://github.com/eijex/factorforge-cds"
            target="_blank"
            rel="noopener noreferrer"
            className="border border-slate-300 text-slate-600 px-6 py-3 rounded-full text-sm font-semibold hover:border-slate-400 transition-colors"
          >
            View on GitHub
          </a>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: page.tsx에 추가 후 확인**

```tsx
import Nav from "@/app/components/Nav";
import AboutStrip from "@/app/components/AboutStrip";
import Hero from "@/app/components/Hero";

export default function Home() {
  return (
    <main>
      <Nav />
      <AboutStrip />
      <Hero />
    </main>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/Hero.tsx src/app/page.tsx
git commit -m "feat: add Hero component"
```

---

### Task 5: AccessOptions 컴포넌트

**Files:**
- Create: `src/app/components/AccessOptions.tsx`

- [ ] **Step 1: AccessOptions 작성**

Bioconda는 PR #65834 머지 전이므로 `soon` 뱃지 포함. 머지 후 `biocondaSoon` 플래그만 `false`로 변경.

```tsx
// src/app/components/AccessOptions.tsx
const biocondaSoon = true; // Bioconda PR #65834 머지 후 false로 변경

const options = [
  {
    label: "pip install factorforge-cds",
    href: "https://pypi.org/project/factorforge-cds/",
    soon: false,
  },
  {
    label: "conda install factorforge-cds",
    href: "https://anaconda.org/bioconda/factorforge-cds",
    soon: biocondaSoon,
  },
  {
    label: "docker pull eijex/factorforge",
    href: "https://hub.docker.com/r/eijex/factorforge",
    soon: false,
  },
];

export default function AccessOptions() {
  return (
    <div className="bg-white border-t border-b border-slate-200 py-5 px-6">
      <div className="max-w-5xl mx-auto flex flex-wrap items-center justify-center gap-3">
        <span className="text-xs font-semibold text-slate-400 uppercase tracking-widest">
          Available via
        </span>
        {options.map((opt) => (
          <a
            key={opt.label}
            href={opt.href}
            target="_blank"
            rel="noopener noreferrer"
            className="flex items-center gap-2 bg-slate-50 border border-slate-200 rounded-lg px-3 py-2 hover:border-slate-300 transition-colors"
          >
            <code className="text-xs text-slate-800">{opt.label}</code>
            {opt.soon && (
              <span className="bg-amber-100 text-amber-800 text-xs font-semibold px-1.5 py-0.5 rounded">
                soon
              </span>
            )}
          </a>
        ))}
        <a
          href="https://factorforge.eijex.com"
          target="_blank"
          rel="noopener noreferrer"
          className="flex items-center gap-2 bg-teal-50 border border-teal-200 rounded-lg px-3 py-2 hover:border-teal-300 transition-colors"
        >
          <span className="text-xs font-semibold text-teal-700">Web App ↗</span>
        </a>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: page.tsx에 추가 후 확인**

```tsx
import Nav from "@/app/components/Nav";
import AboutStrip from "@/app/components/AboutStrip";
import Hero from "@/app/components/Hero";
import AccessOptions from "@/app/components/AccessOptions";

export default function Home() {
  return (
    <main>
      <Nav />
      <AboutStrip />
      <Hero />
      <AccessOptions />
    </main>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/AccessOptions.tsx src/app/page.tsx
git commit -m "feat: add AccessOptions component with Bioconda soon badge"
```

---

### Task 6: StatsBar 컴포넌트

**Files:**
- Create: `src/app/components/StatsBar.tsx`

- [ ] **Step 1: StatsBar 작성**

버전 수치는 릴리즈마다 이 파일의 `stats` 배열만 업데이트하면 됨.

```tsx
// src/app/components/StatsBar.tsx
const stats = [
  { value: "0.76", label: "mean CAI" },
  { value: "100%", label: "AA identity" },
  { value: "3,876", label: "CDS benchmarked" },
  { value: "v3.1.8", label: "latest release" },
];

export default function StatsBar() {
  return (
    <div
      className="py-7 px-6"
      style={{ background: "linear-gradient(90deg, #134e4a, #14532d)" }}
    >
      <div className="max-w-5xl mx-auto grid grid-cols-2 sm:grid-cols-4 gap-6 text-center">
        {stats.map((stat, i) => (
          <div key={i}>
            <div className="text-2xl font-extrabold text-teal-300 mb-1">
              {stat.value}
            </div>
            <div className="text-xs text-teal-200/70">{stat.label}</div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: page.tsx에 추가 후 확인**

```tsx
import Nav from "@/app/components/Nav";
import AboutStrip from "@/app/components/AboutStrip";
import Hero from "@/app/components/Hero";
import AccessOptions from "@/app/components/AccessOptions";
import StatsBar from "@/app/components/StatsBar";

export default function Home() {
  return (
    <main>
      <Nav />
      <AboutStrip />
      <Hero />
      <AccessOptions />
      <StatsBar />
    </main>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/StatsBar.tsx src/app/page.tsx
git commit -m "feat: add StatsBar component"
```

---

### Task 7: Products 컴포넌트

**Files:**
- Create: `src/app/components/Products.tsx`

- [ ] **Step 1: Products 작성**

```tsx
// src/app/components/Products.tsx
const products = [
  {
    name: "FactorForge",
    tag: "CDS Design Engine",
    tagColor: "text-teal-600",
    description:
      "Constraint-based CDS optimization for plant expression. Maximize CAI, control GC%, eliminate PolyA signals. MoClo/Golden Gate-ready output.",
    code: "pip install factorforge-cds",
    codeColor: "text-teal-700",
    primary: { label: "Docs ↗", href: "https://factorforge.eijex.com/docs" },
    secondary: { label: "GitHub", href: "https://github.com/eijex/factorforge-cds" },
    primaryBg: "bg-teal-600 hover:bg-teal-700",
  },
  {
    name: "Eijex MCP",
    tag: "AI Agent Access",
    tagColor: "text-teal-900",
    description:
      "Use FactorForge through Claude Code, Cursor, and any MCP-compatible AI agent. No installation required.",
    code: "mcp.eijex.com",
    codeColor: "text-teal-900",
    primary: { label: "Connect ↗", href: "https://mcp.eijex.com" },
    secondary: { label: "Docs", href: "https://mcp.eijex.com/docs" },
    primaryBg: "bg-teal-900 hover:bg-teal-800",
  },
];

export default function Products() {
  return (
    <section id="products" className="py-16 px-6 bg-white">
      <div className="max-w-5xl mx-auto">
        <div className="text-center mb-10">
          <p className="text-xs font-bold text-slate-400 uppercase tracking-widest mb-2">
            Products
          </p>
          <h2 className="text-2xl font-bold text-slate-900">
            Tools built for the bench and the pipeline.
          </h2>
        </div>
        <div className="grid grid-cols-1 sm:grid-cols-2 gap-5 max-w-3xl mx-auto">
          {products.map((p) => (
            <div
              key={p.name}
              className="bg-slate-50 border border-slate-200 rounded-2xl p-7"
            >
              <h3 className="font-extrabold text-lg text-slate-900 mb-1">{p.name}</h3>
              <p className={`text-xs font-semibold uppercase tracking-wide mb-3 ${p.tagColor}`}>
                {p.tag}
              </p>
              <p className="text-xs text-slate-500 leading-relaxed mb-5">{p.description}</p>
              <code className={`block bg-slate-100 rounded-md px-3 py-2 text-xs mb-5 ${p.codeColor}`}>
                {p.code}
              </code>
              <div className="flex gap-2">
                <a
                  href={p.primary.href}
                  target="_blank"
                  rel="noopener noreferrer"
                  className={`flex-1 text-center text-white text-xs font-bold py-2 rounded-lg transition-colors ${p.primaryBg}`}
                >
                  {p.primary.label}
                </a>
                <a
                  href={p.secondary.href}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex-1 text-center border border-slate-200 text-slate-500 text-xs py-2 rounded-lg hover:border-slate-300 transition-colors"
                >
                  {p.secondary.label}
                </a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: page.tsx에 추가 후 확인**

```tsx
import Nav from "@/app/components/Nav";
import AboutStrip from "@/app/components/AboutStrip";
import Hero from "@/app/components/Hero";
import AccessOptions from "@/app/components/AccessOptions";
import StatsBar from "@/app/components/StatsBar";
import Products from "@/app/components/Products";

export default function Home() {
  return (
    <main>
      <Nav />
      <AboutStrip />
      <Hero />
      <AccessOptions />
      <StatsBar />
      <Products />
    </main>
  );
}
```

- [ ] **Step 3: 커밋**

```bash
git add src/app/components/Products.tsx src/app/page.tsx
git commit -m "feat: add Products component"
```

---

### Task 8: Footer 컴포넌트

**Files:**
- Create: `src/app/components/Footer.tsx`

- [ ] **Step 1: Footer 작성**

```tsx
// src/app/components/Footer.tsx
export default function Footer() {
  return (
    <footer
      className="px-6 pt-10 pb-6"
      style={{ background: "linear-gradient(90deg, #134e4a, #14532d)" }}
    >
      <div className="max-w-5xl mx-auto">
        <div className="grid grid-cols-1 sm:grid-cols-4 gap-8 mb-8">
          {/* Brand */}
          <div className="sm:col-span-1">
            <div className="font-extrabold text-lg text-teal-300 mb-2">eijex</div>
            <p className="text-xs text-teal-200/70 leading-relaxed">
              Open-source computational tools<br />for plant synthetic biology.
            </p>
          </div>
          {/* Products */}
          <div>
            <p className="text-xs font-bold text-teal-400 uppercase tracking-widest mb-3">
              Products
            </p>
            <ul className="space-y-2">
              {[
                { label: "FactorForge", href: "https://factorforge.eijex.com" },
                { label: "Eijex MCP", href: "https://mcp.eijex.com" },
              ].map((l) => (
                <li key={l.label}>
                  <a
                    href={l.href}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-xs text-teal-200/70 hover:text-teal-200 transition-colors"
                  >
                    {l.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          {/* Resources */}
          <div>
            <p className="text-xs font-bold text-teal-400 uppercase tracking-widest mb-3">
              Resources
            </p>
            <ul className="space-y-2">
              {[
                { label: "Documentation", href: "https://factorforge.eijex.com/docs" },
                { label: "GitHub", href: "https://github.com/eijex/factorforge-cds" },
                { label: "PyPI", href: "https://pypi.org/project/factorforge-cds/" },
                { label: "Zenodo / DOI", href: "https://doi.org/10.5281/zenodo.20407331" },
              ].map((l) => (
                <li key={l.label}>
                  <a
                    href={l.href}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-xs text-teal-200/70 hover:text-teal-200 transition-colors"
                  >
                    {l.label}
                  </a>
                </li>
              ))}
            </ul>
          </div>
          {/* Citation */}
          <div>
            <p className="text-xs font-bold text-teal-400 uppercase tracking-widest mb-3">
              Citation
            </p>
            <code className="block bg-black/20 rounded-lg px-3 py-3 text-xs text-teal-200/80 leading-relaxed font-mono">
              factorforge-cds v3.1.8<br />
              DOI: 10.5281/zenodo.<br />
              20407331
            </code>
          </div>
        </div>
        <div className="border-t border-white/10 pt-4 flex justify-between items-center">
          <span className="text-xs text-teal-400/60">© 2026 eijex</span>
          <span className="text-xs text-teal-400/60">MIT / AGPL-3.0</span>
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 2: page.tsx 최종 조합**

```tsx
import Nav from "@/app/components/Nav";
import AboutStrip from "@/app/components/AboutStrip";
import Hero from "@/app/components/Hero";
import AccessOptions from "@/app/components/AccessOptions";
import StatsBar from "@/app/components/StatsBar";
import Products from "@/app/components/Products";
import Footer from "@/app/components/Footer";

export default function Home() {
  return (
    <main>
      <Nav />
      <AboutStrip />
      <Hero />
      <AccessOptions />
      <StatsBar />
      <Products />
      <Footer />
    </main>
  );
}
```

- [ ] **Step 3: 전체 페이지 브라우저 확인**

`http://localhost:3000` 에서 모든 섹션 확인:
- Nav sticky 동작
- Hero gradient
- Access Options 뱃지 + Bioconda "soon" 표시
- StatsBar 2×2 그리드 (모바일) / 4열 (데스크탑)
- Products 2열 카드
- Footer 3+1 컬럼

- [ ] **Step 4: 커밋**

```bash
git add src/app/components/Footer.tsx src/app/page.tsx
git commit -m "feat: add Footer component and assemble full page"
```

---

### Task 9: 반응형 + Lighthouse 검증

**Files:**
- Modify: 필요 시 각 컴포넌트 className 조정

- [ ] **Step 1: 모바일(375px) 확인**

브라우저 DevTools → Device Toolbar → iPhone SE (375px) 선택.

체크리스트:
- [ ] Nav: "Get started" 버튼만 표시, 메뉴 링크 숨김
- [ ] Hero: 버튼 세로 스택 (`flex-col sm:flex-row` 적용됨)
- [ ] AccessOptions: 세로 스택 (`flex-wrap` 적용됨)
- [ ] StatsBar: 2×2 그리드 (`grid-cols-2 sm:grid-cols-4` 적용됨)
- [ ] Products: 1열 (`grid-cols-1 sm:grid-cols-2` 적용됨)
- [ ] Footer: 1열 스택 (`grid-cols-1 sm:grid-cols-4` 적용됨)

- [ ] **Step 2: 빌드 + 타입 체크**

```bash
npm run build
```

Expected: `✓ Compiled successfully` — 에러 없음.

- [ ] **Step 3: Lighthouse 검사**

```bash
npm run build && npm start
```

브라우저에서 `http://localhost:3000` → DevTools → Lighthouse → "Analyze page load" 실행.

목표:
- Performance ≥ 90
- Accessibility ≥ 90
- Best Practices ≥ 90
- SEO ≥ 90

- [ ] **Step 4: 커밋**

```bash
git add -A
git commit -m "chore: verify responsiveness and Lighthouse scores"
```

---

### Task 10: Vercel 배포

**Files:**
- Create: `vercel.json`

- [ ] **Step 1: vercel.json 생성**

```json
{
  "framework": "nextjs"
}
```

- [ ] **Step 2: GitHub 레포 생성 및 푸시**

```bash
gh repo create eijex/eijex-web --public --source=. --remote=origin --push
```

- [ ] **Step 3: Vercel 연결**

Vercel 대시보드 → "Add New Project" → `eijex/eijex-web` import → 기본 설정으로 Deploy.

- [ ] **Step 4: 커스텀 도메인 연결**

Vercel 프로젝트 → Settings → Domains → `www.eijex.com` 추가.  
DNS: CNAME `www` → `cname.vercel-dns.com`

- [ ] **Step 5: 배포 확인**

`https://www.eijex.com` 접속 → 전체 섹션 정상 렌더링 확인.

- [ ] **Step 6: 커밋**

```bash
git add vercel.json
git commit -m "chore: add vercel.json for deployment"
git push
```

---

## Acceptance Criteria 체크리스트

- [ ] www.eijex.com Vercel 배포 완료
- [ ] 모바일(375px) + 데스크탑(1280px) 모두 정상 렌더링
- [ ] 모든 외부 링크 (GitHub, PyPI, Zenodo, factorforge.eijex.com, mcp.eijex.com) 작동
- [ ] Bioconda "soon" 뱃지 표시 (`biocondaSoon = true`)
- [ ] Lighthouse Performance ≥ 90
- [ ] 미공개 제품명 코드/텍스트 어디에도 없음
- [ ] `npm run build` 에러 없음
